#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,m;
    while(cin>>n>>m){
        getchar();
        int b,sum=0;
        string a;
        map<string,int>mp
        while(n--){
            cin>>a>>b;
            mp[a]=b;
        }
        string word;
        getline(cin,word)
        stringstream s(word);
        while(s>>word)
        {
            if(word.size()==1)
        }
        cout<<endl;
    }
}

